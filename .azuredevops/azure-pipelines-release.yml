trigger:
  branches:
    include:
      - Develop   # switch to main when ready
  tags:
    include:
      - Abgabe_*

pr: none

pool:
  vmImage: ubuntu-latest

variables:
  buildConfiguration: Release
  artifactBaseName: OrderManagement

steps:
- checkout: self
  clean: true

- task: UseDotNet@2
  inputs:
    packageType: sdk
    version: 9.x

- script: dotnet restore
  displayName: Restore

- script: dotnet format --verify-no-changes
  displayName: Verify formatting

- script: dotnet test --configuration $(buildConfiguration) --no-restore
  displayName: Run tests

# Create ZIP directly from repo (no deleting .git/bin/obj required)
# If size becomes an issue, we can filter later.
- task: ArchiveFiles@2
  displayName: Create release ZIP
  inputs:
    rootFolderOrFile: '$(Build.SourcesDirectory)'
    includeRootFolder: false
    archiveType: zip
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip'
    replaceExistingArchive: true

# Keep Azure artifact (nice fallback)
- task: PublishBuildArtifacts@1
  displayName: Publish Azure artifact
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip'
    ArtifactName: ReleasePackage

# Create GitHub Release + upload the zip as asset
- script: |
    set -euo pipefail

    ZIP_PATH="$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip"

    # For tag builds, Build.SourceBranchName == the tag name, e.g. Abgabe_Aufgabe123
    TAG_NAME="$(Build.SourceBranchName)"

    # IMPORTANT: set this explicitly to avoid ambiguity
    # Format: owner/repo
    GH_REPO="zbw-online/it27.sw.group05"

    # Install GitHub CLI
    sudo apt-get update
    sudo apt-get install -y gh

    # Authenticate
    echo "$(GITHUB_TOKEN)" | gh auth login --with-token

    # Create release if missing
    if ! gh release view "$TAG_NAME" --repo "$GH_REPO" > /dev/null 2>&1; then
      gh release create "$TAG_NAME" \
        --repo "$GH_REPO" \
        --title "$TAG_NAME" \
        --notes "Automated release created by Azure DevOps pipeline."
    fi

    # Upload/replace asset
    gh release upload "$TAG_NAME" "$ZIP_PATH" --repo "$GH_REPO" --clobber
  displayName: Create GitHub Release + upload ZIP
  env:
    GITHUB_TOKEN: $(GITHUB_TOKEN)
