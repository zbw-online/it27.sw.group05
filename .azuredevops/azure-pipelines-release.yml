trigger:
  branches:
    include:
      - main
      - Feature*
pr: none

pool:
  vmImage: ubuntu-24.04

variables:
- group: github-secret-Public-Repository   # must contain secret variable: GITHUB_TOKEN

- name: buildConfiguration
  value: Release

- name: artifactBaseName
  value: OrderManagement

- name: githubRepo
  value: zbw-online/it27.sw.group05

# Folder containing your .docx files
- name: docsFolder
  value: '$(Build.SourcesDirectory)/docs'

# Output folder for generated PDFs
- name: pdfOutputFolder
  value: '$(Build.SourcesDirectory)/generated-pdf'

steps:
- checkout: self
  clean: true

- task: UseDotNet@2
  inputs:
    packageType: sdk
    version: 9.x

- script: dotnet restore
  displayName: Restore

- script: dotnet format --verify-no-changes
  displayName: Verify formatting

- script: dotnet test --configuration $(buildConfiguration) --no-restore
  displayName: Run tests

- script: |
    set -euo pipefail

    sudo apt-get update
    sudo apt-get install -y libreoffice

    mkdir -p "$(pdfOutputFolder)"

    if [ ! -d "$(docsFolder)" ]; then
      echo "Docs folder not found: $(docsFolder)"
      exit 1
    fi

    mapfile -d '' files < <(find "$(docsFolder)" -type f \( -iname "*.docx" \) -print0)

    if [ ${#files[@]} -eq 0 ]; then
      echo "No .docx files found in $(docsFolder)"
      exit 1
    fi

    for file in "${files[@]}"; do
      echo "Converting: $file"
      libreoffice --headless --convert-to pdf --outdir "$(pdfOutputFolder)" "$file"
    done

    echo "Generated PDFs:"
    ls -la "$(pdfOutputFolder)"
  displayName: Convert DOCX to PDF

- task: ArchiveFiles@2
  displayName: Create release ZIP
  inputs:
    rootFolderOrFile: '$(Build.SourcesDirectory)'
    includeRootFolder: false
    archiveType: zip
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip'
    replaceExistingArchive: true

- task: PublishBuildArtifacts@1
  displayName: Publish Azure artifact
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip'
    ArtifactName: ReleasePackage

- script: |
    set -euo pipefail

    ZIP_PATH="$(Build.ArtifactStagingDirectory)/$(artifactBaseName)_$(Build.SourceBranchName)_$(Build.BuildNumber).zip"
    TAG_NAME="$(Build.SourceBranchName)"
    GH_REPO="$(githubRepo)"

    echo "Repo: $GH_REPO"
    echo "Tag:  $TAG_NAME"
    echo "Zip:  $ZIP_PATH"

    TOKEN="${GH_TOKEN:-${GITHUB_TOKEN:-}}"
    if [ -z "$TOKEN" ]; then
      echo "ERROR: No GitHub token injected. Check variable group linking + pipeline permissions."
      exit 1
    fi
    export GH_TOKEN="$TOKEN"
    export GITHUB_TOKEN="$TOKEN"

    gh api "repos/$GH_REPO" >/dev/null

    if ! gh release view "$TAG_NAME" --repo "$GH_REPO" >/dev/null 2>&1; then
      gh release create "$TAG_NAME" \
        --repo "$GH_REPO" \
        --title "$TAG_NAME" \
        --notes "Automated release created by Azure DevOps pipeline."
    fi

    gh release upload "$TAG_NAME" "$ZIP_PATH" --repo "$GH_REPO" --clobber
  displayName: Create GitHub Release + upload ZIP
  env:
    GH_TOKEN: $(GITHUB_TOKEN)
    GITHUB_TOKEN: $(GITHUB_TOKEN)
